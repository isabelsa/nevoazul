export interface ITheme {
  colors: {
    brand: {
      default: string
    }
    beige: {
      default: string
    }
    blue: {
      default: string
    }
    brown: {
      default: string
      dark: string
    }
    green: {
      default: string
    }
    red: {
      default: string
    }
    background: string
    foreground: string
  }
}
